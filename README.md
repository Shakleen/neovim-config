# Ishfar's NVChad NeoVim Configuration

## Pre-requisite
Install the following packages. Otherwise, the configuration won't work properly:

```bash
sudo pacman -S --needed neovim unzip luarocks xclip wl-clipboard go npm
```
